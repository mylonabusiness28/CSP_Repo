channel v, h : {1..3}.{1..3}.{1..3}

MIN(a, b) = if a<b then a else b -- isso seria um laço de repetição para 

MAX(a, b) = if a>=b then a else b

-- perguntar para o professor se como declarar matrizes em CSP
-- como faz para atribuir os valores nas posições da matriz 